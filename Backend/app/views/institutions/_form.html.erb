<%= form_for(@institution) do |f| %>
  <% if @institution.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@institution.errors.count, "error") %> prohibited this institution from being saved:</h2>

      <ul>
      <% @institution.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label 'Ime' %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label 'Alarmantna razina [u dozama]' %><br>
    <%= f.text_field :low_level %>
  </div>

  <%= fields_for :address, @institution.address do |address_fields| %>
    Grad: <br> <%= address_fields.text_field :city %> <br>
    Država: <br> <%= address_fields.text_field :state %><br/>
  <% end %>

<br>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
